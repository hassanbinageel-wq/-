# جدول التوافق — مونيتور Sony

هذا المستند يوضّح **ما يمكن لتطبيق Android مستقل** أن يفعله فعليًا مع كاميرات Sony
عبر Wi‑Fi، ويصنّف كل وظيفة إلى:

- ✅ **مؤكَّدة بالبروتوكول**: البروتوكول (Sony ScalarWebAPI / Camera Remote API) يوثّق هذه الوظيفة، والكود ينفّذها. يبقى تأكيدها على موديل محدّد رهنَ ظهورها في `getAvailableApiList` القادمة من الكاميرا نفسها.
- 🧪 **تحتاج اختبارًا على كاميرا فعلية**: منطقيًا ممكنة، لكن دعم الموديل المحدّد لها غير مؤكَّد من مصدر رسمي ويجب قياسه على الكاميرا.
- ❌ **غير مدعومة عبر هذا الاتصال**: لا يوفّرها البروتوكول المتاح لتطبيق مستقل، أو محصورة في تطبيق Sony الرسمي / اتصال USB / موديلات أحدث.

> **مبدأ أساسي:** التطبيق يبني الأزرار المتاحة من `getAvailableApiList` التي ترسلها الكاميرا لحظة الاتصال. لا نفترض أن وظيفة موجودة في تطبيق Sony الرسمي (Imaging Edge / Creators' App) متاحة تلقائيًا لتطبيق مستقل.

---

## 1) خلاصة مسارات الاتصال

| المسار | يعمل على Android مستقل؟ | الملاحظة |
|---|---|---|
| **Camera Remote SDK** (الرسمي الحالي) | ❌ | SDK لسطح المكتب بلغة C++ ولا يعمل على Android. |
| **Camera Remote API / ScalarWebAPI** (JSON عبر HTTP + بث liveview) | ✅ مُختبَر على a7 III | المسار الأول الذي يجرّبه التطبيق. الكاميرا عادةً على `192.168.122.1:8080`. الوظائف تُكتشف من `getAvailableApiList`. |
| **PTP/IP بامتداد Sony (SDIO)** — منفذ 15740 | 🧪 مُنفَّذ (تجريبي) | يُجرَّب تلقائيًا إن لم توجد ScalarWebAPI (حال أغلب الموديلات بعد 2020). بُني من التوثيق المفتوح للبروتوكول (libgphoto2) واختُبر على محاكي كاميرا فقط — **يحتاج اختبارًا على كل موديل حقيقي**. |
| **UPnP/DLNA ContentDirectory** (وضع «إرسال إلى الهاتف الذكي») | 🧪 مُنفَّذ | لاستعراض ملفات البطاقة واستيرادها. قراءة فقط. |

**المصادر:**
- Camera Remote SDK (الموديلات وأنظمة التشغيل): <https://support.d-imaging.sony.co.jp/app/sdk/en/index.html>
- تحديث SDK ودعم الاتصال اللاسلكي للموديلات الأحدث: <https://pro.sony/ue_US/press/sdk-updates-remote-functions-compatibility-support>
- إعلان Sony عن التحكم عبر ISO‑PTP بامتداد خاص: <https://www.sony.eu/presscentre/sony-enables-remote-control-of-a-wide-camera-range-through-iso-ptp-protocol-through-proprietary-extension>
- Camera Remote Command: <https://support.d-imaging.sony.co.jp/app/cameraremotecommand/en/index.html>
- خبر إطلاق SDK (DPReview): <https://www.dpreview.com/news/0419921057/sony-releases-software-development-kit-sdk-for-remote-camera-access>
- تطبيق طرف ثالث اختُبر مع a7 III عبر ScalarWebAPI (كمرجع عملي، وليس مصدرًا رسميًا): <https://github.com/jongan69/expo-sony-camera>
- نقاش liveview لسلسلة Alpha عبر gphoto2: <https://github.com/gphoto/gphoto2/issues/156>

---

## 2) Sony a7 III (ILCE‑7M3) — عبر ScalarWebAPI

نتائج مقيسة على كاميرا المستخدم (سجل التشخيص): الاكتشاف عبر SSDP، و50 وظيفة في `getAvailableApiList`.

| الوظيفة | الحالة | التفصيل |
|---|---|---|
| الاتصال والاكتشاف (SSDP + DD.xml) | ✅ مُختبَرة | |
| البث الحي | ✅ مُختبَرة | 640×360 بحوالي 25 إطار/ث (مقيس). التأخير يُقاس ولا نصفه بأنه «بلا تأخير». |
| قراءة/تغيير ISO والغالق والفتحة | ✅ مُختبَرة | تتغيّر القيمة المعروضة فقط بعد تأكيد الكاميرا عبر `getEvent`. |
| توازن الأبيض + حرارة اللون (كلفن) | 🧪 | `setWhiteBalance` موجودة؛ الكلفن عبر وضع «Color Temperature» بالنطاق الذي تعلنه الكاميرا. |
| التقاط الصور | 🧪 (أُصلح في 0.6.0) | `actTakePicture` مع إيقاف الاستطلاع مؤقتًا، ثم إعادة المحاولة بضغط نصفي عند الفشل، ورمز الخطأ الحقيقي يظهر للمستخدم (40400 = فشل الالتقاط غالبًا بسبب التركيز). |
| التركيز باللمس على نقطة | ❌ | `setTouchAFPosition` **غير موجودة** في قائمة a7 III. البديل المنفّذ: ضغط نصفي = تركيز تلقائي على منطقة التركيز المضبوطة في الكاميرا. |
| تعويض التعريض | ❌ | `setExposureCompensation` غير موجودة في قائمة a7 III (قراءة فقط). |
| تسجيل الفيديو | 🧪 | `startMovieRec` موجودة في القائمة؛ يجب أن يكون الدايل على وضع الفيديو. |
| Picture Profile / وضع التصوير | ❌ | لا توجد وظائف لها عبر هذا المسار (`setShootMode` غير موجودة). |
| بطارية الكاميرا | ❌ غالبًا | a7 III لا ترسل مستوى البطارية في `getEvent`. |
| صورة المعاينة بعد الالتقاط (postview) | 🧪 | تُنزَّل وتظهر في «لقطات هذه الجلسة». |
| ملفات البطاقة أثناء التحكم | ❌ | `avContent` غير متاحة في وضع «التحكّم بالهاتف». البديل: وضع «إرسال إلى الهاتف الذكي» ← UPnP (🧪). |

### حدود أدوات المراقبة الناتجة عن البث
جميع الأدوات (Histogram, Waveform, Zebra, False Color, RGB Parade, Focus Peaking) **تُحسب من إطار المعاينة المضغوط (JPEG)** وليست من RAW/Log. القيم تقريبية (0–100%) وليست وحدات مُعايَرة. LUT **للمعاينة فقط** (باستيفاء ثلاثي trilinear منذ 0.6.0)، وضجيج JPEG في الإضاءة المنخفضة/ISO العالي يظهر أوضح بعد LUT تباين عالٍ.

---

## 3) الموديلات الأحدث — عبر PTP/IP (تجريبي)

المتطلبات على الكاميرا: MENU ← الشبكة ← **التحكم عن بعد بالكمبيوتر (PC Remote): تشغيل**، وطريقة الاتصال **Wi‑Fi Direct أو نقطة وصول**، و**إيقاف «مصادقة الوصول / Access Authen.»** إن وُجدت (عند تفعيلها تطلب الكاميرا مصادقة مشفّرة غير منفّذة بعد). في بعض الموديلات: «أولوية الإعداد عن بُعد» = الكمبيوتر، ليتمكّن التطبيق من تغيير القيم.

| الموديل | بث حي | التقاط | فيديو | ISO/غالق/فتحة/WB/كلفن | تركيز | الحالة |
|---|---|---|---|---|---|---|
| a7 IV، a7R IV/V، a7C/a7C II/a7CR | 🧪 | 🧪 | 🧪 | 🧪 | ضغط نصفي | منفَّذ، يحتاج اختبارًا |
| a7S III، a1، a9 II/III | 🧪 | 🧪 | 🧪 | 🧪 | ضغط نصفي | منفَّذ، يحتاج اختبارًا |
| FX3، FX30 | 🧪 | 🧪 | 🧪 | 🧪 | ضغط نصفي | منفَّذ، يحتاج اختبارًا |
| ZV‑E1، ZV‑E10 II، a6700 | 🧪 | 🧪 | 🧪 | 🧪 | ضغط نصفي | منفَّذ، يحتاج اختبارًا |
| a7 III (إن فعّلت PC Remote عبر Wi‑Fi) | 🧪 | 🧪 | 🧪 | 🧪 | ضغط نصفي | المسار الافتراضي لها هو ScalarWebAPI |

ما يُعرض من أزرار وقيم يُبنى من الخصائص التي تعلنها الكاميرا نفسها عبر `GetAllExtDevicePropInfo (0x9209)`؛ الخاصية غير المعلنة أو المقفولة تبقى معطّلة مع سبب.

> تُحدَّث هذه الخانات إلى ✅ أو ❌ بعد كل اختبار فعلي على الجهاز — أرسل سجل «التشخيص» من التطبيق.
